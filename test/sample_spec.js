var assert = require('assert');

describe('Topic', function () {
  
  describe('Subtopic', function () {
    it('should behave this way', function (done) {
      assert.ok(true);
      done();
    });
  });
});